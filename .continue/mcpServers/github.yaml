name: GitHub
version: 0.0.1
schema: v1
mcpServers:
  - name: github
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-github"
      - --github-token
      - ${process.env.GITHUB_TOKEN}